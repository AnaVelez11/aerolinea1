package iam.aerolinea.repositorio;

import iam.aerolinea.modelo.CarroEmbarque;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CarroEmbarqueRepositorio extends JpaRepository<CarroEmbarque, Long> {
}
